
from icecream import ic
from multDiv import mult, div
from sums import sum
from name import name

if __name__ == "__main__":
 

    ic (mult(2, 4))
    ic (div(10, 2))
    ic (sum(6, 2))
    ic (sum(2, 5 , 10))
    ic (print("segev"))

